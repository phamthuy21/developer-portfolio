# Create Module Workflow

**Trigger:** When adding a completely new domain to the NestJS backend.

## Steps
1. Use `nest g module [name]`.
2. Use `nest g controller [name]`.
3. Use `nest g service [name]`.
4. Register the module in `app.module.ts`.
5. Scaffold basic CRUD methods in the controller and service.
