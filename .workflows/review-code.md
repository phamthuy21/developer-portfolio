# Review Code Workflow

**Trigger:** When a phase or feature is complete before generating the phase report.

## Steps
1. Check against `CODING_STANDARD.md`.
2. Verify TypeScript strictness (no implicit `any`).
3. Ensure no placeholder code is left behind.
4. Verify responsiveness and accessibility.
5. Fix linting errors.
