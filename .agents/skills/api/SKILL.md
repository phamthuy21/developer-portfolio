---
name: api
description: REST API design and integration skills.
---
# API Skill

1. Maintain a clean contract between Frontend and Backend (defined in `docs/api/endpoints.md`).
2. Use `TanStack Query` on the frontend for data fetching hooks.
3. Generate and use standard TypeScript DTOs.
4. Keep controllers thin; handle complex mapping in services.
