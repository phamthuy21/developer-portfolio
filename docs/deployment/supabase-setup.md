# Supabase Configuration Guide

1. Create a new Supabase Project.
2. Go to **Database** -> **Connection Pooling**.
3. Enable connection pooling. Copy the `DATABASE_URL` and `DIRECT_URL`.
4. Ensure password is secure and save it.
